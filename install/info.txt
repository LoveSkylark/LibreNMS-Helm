usefull short cuts:
 k=kubectl
    kl                            // list all resoucess in namsepace
    kall                          // list all resoucess in all namsepaces
    kn <namespace>                //switch to another namespace
    knd <defaul_namespace>        //switch to back to default
    kbash <pod_name>              // jump into a pod

 h=helm
    hl                            // list all releases in namsepace
    hall                          // list all releases in all namsepaces
    hin                           // helm install
    hup                           // helm upgrade
    hun                           // helm uninstall

k9s  = manage the kube cluster"

nms-edit = change cluster configuration"
nms-help = libreNMS cli commands

lnms = libreNMS cli commands