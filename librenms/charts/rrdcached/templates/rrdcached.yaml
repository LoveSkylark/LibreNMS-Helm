---
apiVersion: v1
kind: Service
metadata:
  name: {{.Values.host }}
  namespace: {{ .Release.Name }}
  labels:
{{ include "common.labels" . | indent 4 }}
spec:
  selector:
    app: {{.Values.host }}
  ports:
    - name: {{.Values.host }}
      protocol: TCP
      port: 42217
      targetPort: {{.Values.host }}

---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: {{.Values.host }}
  namespace: {{ .Release.Name }}
  labels:
{{ include "common.labels" . | indent 4 }}
    app: {{.Values.host }}
spec:
  serviceName: {{.Values.host }}
  replicas: {{.Values.replicas}}
  selector:
    matchLabels:
      app: {{.Values.host }}
  template:
    metadata:
      labels:
        app: {{.Values.host }}
    spec:
      restartPolicy: Always
      containers:
        - name: {{.Values.host }}
          image: {{.Values.image}}
          envFrom:
            - configMapRef:
                name: {{.Values.host }}
          securityContext:
            capabilities:
              add:
                - NET_ADMIN
                - NET_RAW
          ports:
            - name: {{.Values.host }}
              containerPort: 42217
              protocol: TCP
          volumeMounts:
            - name: application
              mountPath: /data/journal
              subPath: journal
            - name: application
              mountPath: /data/db
              subPath:  rrd
      volumes:
        - name: application
          persistentVolumeClaim:
            claimName: application
